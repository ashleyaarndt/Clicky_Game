import React from "react";

import "./Header.css";

const Header = () => (
  <div className="banner text-center d-flex align-items-center justify-content-center">
    <p className="m-0">
      Click on an image to earn points, but don't click on any one more than
      once!
    </p>
  </div>
);

export default Header;
